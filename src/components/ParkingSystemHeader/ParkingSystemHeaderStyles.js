import { css } from '@emotion/css';

export default {
  headerContainer: css({
    width: '100%',
    marginTop: '16px',
  }),
  headerTitle: css({
    fontSize: '24px',
    color: '#FFFFFF',
  }),
  headerDescription: css({
    fontSize: '20px',
    color: '#FFFFFF',
  })
}
